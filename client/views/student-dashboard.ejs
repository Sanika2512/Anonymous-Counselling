<%- include('partials/header') %>

<link href="https://fonts.googleapis.com/css2?family=Sora:wght@300;400;500;600;700;800&family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;1,9..40,300&display=swap" rel="stylesheet">

<style>
/* ===== ROOT VARIABLES ===== */
:root {
  --brand: #4361ee;
  --brand-2: #7b2ff7;
  --brand-light: #e8edff;
  --accent: #06d6a0;
  --accent-warm: #ff6b6b;
  --gold: #fbbf24;
  --surface: rgba(255,255,255,0.72);
  --surface-2: rgba(255,255,255,0.45);
  --border: rgba(67,97,238,0.13);
  --text-primary: #0f1628;
  --text-secondary: #5a6482;
  --text-muted: #9aa3c0;
  --sidebar-bg: linear-gradient(160deg, #0f1628 0%, #1a2550 60%, #0f1628 100%);
  --glow: 0 0 40px rgba(67,97,238,0.18);
  --radius: 20px;
  --radius-sm: 12px;
  --transition: 0.35s cubic-bezier(0.34,1.4,0.64,1);
}

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: 'DM Sans', sans-serif;
  background: #f0f2f9;
  min-height: 100vh;
  overflow-x: hidden;
}

/* ===== ANIMATED BACKGROUND ===== */
body::before {
  content: '';
  position: fixed;
  inset: 0;
  background:
    radial-gradient(ellipse 900px 700px at 80% -10%, rgba(67,97,238,0.08) 0%, transparent 70%),
    radial-gradient(ellipse 600px 500px at -10% 80%, rgba(123,47,247,0.07) 0%, transparent 70%),
    radial-gradient(ellipse 500px 400px at 50% 110%, rgba(6,214,160,0.05) 0%, transparent 70%);
  pointer-events: none;
  z-index: 0;
}

/* ===== SIDEBAR ===== */
.sidebar {
  background: var(--sidebar-bg);
  border-right: none;
  box-shadow: 4px 0 40px rgba(10,15,40,0.3);
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
  padding: 32px 16px;
  position: sticky;
  top: 0;
  transition: all 0.3s ease;
  z-index: 100;
  overflow: hidden;
}

/* Sidebar inner glow orbs */
.sidebar::before {
  content: '';
  position: absolute;
  top: -60px; left: -60px;
  width: 200px; height: 200px;
  background: radial-gradient(circle, rgba(67,97,238,0.25) 0%, transparent 70%);
  pointer-events: none;
}

.sidebar::after {
  content: '';
  position: absolute;
  bottom: -40px; right: -40px;
  width: 180px; height: 180px;
  background: radial-gradient(circle, rgba(123,47,247,0.2) 0%, transparent 70%);
  pointer-events: none;
}

.message-dots { display: none !important; }

.user-profile-container {
  width: 100%;
  padding: 0 12px;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 14px;
  position: relative;
  z-index: 1;
}

/* ===== AVATAR ===== */
.avatar-circle {
  width: 90px;
  height: 90px;
  border-radius: 50%;
  background: linear-gradient(135deg, #4361ee, #7b2ff7);
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  position: relative;
  transition: var(--transition);
  border: 3px solid rgba(255,255,255,0.15);
  box-shadow: 0 0 0 6px rgba(67,97,238,0.18), 0 20px 40px rgba(0,0,0,0.35);
}

.avatar-circle::before {
  content: '';
  position: absolute;
  inset: -4px;
  border-radius: 50%;
  background: conic-gradient(from 0deg, #4361ee, #7b2ff7, #06d6a0, #4361ee);
  z-index: -1;
  opacity: 0;
  transition: opacity 0.4s ease;
  animation: spin-slow 3s linear infinite;
}

.avatar-circle:hover::before { opacity: 1; }

@keyframes spin-slow { to { transform: rotate(360deg); } }

.avatar-circle:hover {
  transform: scale(1.08) translateY(-3px);
  box-shadow: 0 0 0 6px rgba(67,97,238,0.35), 0 25px 50px rgba(0,0,0,0.4);
}

.avatar-circle i {
  font-size: 2.8rem;
  color: white;
  transition: transform 0.5s ease;
  filter: drop-shadow(0 2px 8px rgba(0,0,0,0.3));
}

.avatar-circle:hover i { transform: scale(1.1); }

/* ===== USERNAME ===== */
.user-profile-container h5 {
  font-family: 'Sora', sans-serif;
  font-size: 1.1rem;
  font-weight: 700;
  color: #fff;
  letter-spacing: 0.3px;
  cursor: pointer;
  transition: color 0.25s;
  text-align: center;
}

.user-profile-container h5:hover { color: #a5b4fc; }

/* ===== STUDENT BADGE ===== */
.badge-student {
  background: rgba(67,97,238,0.25);
  color: #a5b4fc;
  padding: 6px 18px;
  border-radius: 50px;
  font-size: 0.82rem;
  font-weight: 600;
  border: 1px solid rgba(165,180,252,0.25);
  cursor: pointer;
  transition: var(--transition);
  letter-spacing: 0.5px;
  text-transform: uppercase;
}

.badge-student:hover {
  background: rgba(67,97,238,0.5);
  color: #fff;
  transform: translateY(-2px);
  box-shadow: 0 8px 20px rgba(67,97,238,0.3);
}

/* ===== SIDEBAR DIVIDER ===== */
.sidebar-divider {
  width: 100%;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.1), transparent);
  margin: 4px 0;
}

/* ===== COUNSELOR STATUS ===== */
.counselor-status-badge {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 12px 16px;
  border-radius: 50px;
  font-size: 0.85rem;
  font-weight: 600;
  width: 100%;
  cursor: pointer;
  transition: var(--transition);
  gap: 8px;
}

.counselor-online {
  background: rgba(6,214,160,0.15);
  color: #06d6a0;
  border: 1px solid rgba(6,214,160,0.3);
}

.counselor-offline {
  background: rgba(255,255,255,0.07);
  color: rgba(255,255,255,0.5);
  border: 1px solid rgba(255,255,255,0.1);
}

.counselor-status-badge:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 20px rgba(0,0,0,0.2);
}

/* ===== PULSE INDICATOR ===== */
.realtime-indicator {
  width: 10px;
  height: 10px;
  background: #06d6a0;
  border-radius: 50%;
  flex-shrink: 0;
  animation: pulse 2s infinite;
  box-shadow: 0 0 0 3px rgba(6,214,160,0.25);
}

@keyframes pulse {
  0% { transform: scale(1); opacity: 1; box-shadow: 0 0 0 3px rgba(6,214,160,0.25); }
  50% { transform: scale(1.2); opacity: 0.8; box-shadow: 0 0 0 6px rgba(6,214,160,0.1); }
  100% { transform: scale(1); opacity: 1; box-shadow: 0 0 0 3px rgba(6,214,160,0.25); }
}

/* ===== AUTO CHATBOT TEXT ===== */
.auto-chatbot-text {
  color: rgba(255,255,255,0.45);
  font-size: 0.78rem;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 7px;
  padding: 8px 14px;
  border-radius: 50px;
  width: 100%;
  cursor: pointer;
  transition: var(--transition);
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(255,255,255,0.07);
}

.auto-chatbot-text:hover {
  background: rgba(123,47,247,0.2);
  color: #c4b5fd;
  border-color: rgba(196,181,253,0.3);
  transform: translateY(-2px);
}

.auto-chatbot-text i { color: #7b2ff7; transition: transform 0.3s ease; }
.auto-chatbot-text:hover i { transform: rotate(15deg) scale(1.2); color: #c4b5fd; }

/* ===== WEATHER WIDGET ===== */
.weather-widget {
  background: linear-gradient(135deg, rgba(67,97,238,0.4) 0%, rgba(123,47,247,0.4) 100%);
  border: 1px solid rgba(165,180,252,0.2);
  backdrop-filter: blur(10px);
  color: white;
  padding: 18px 20px;
  border-radius: var(--radius-sm);
  display: flex;
  align-items: center;
  justify-content: space-between;
  width: 100%;
  cursor: pointer;
  transition: var(--transition);
  box-shadow: 0 8px 24px rgba(0,0,0,0.25);
  position: relative;
  overflow: hidden;
}

.weather-widget::before {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(135deg, rgba(255,255,255,0.08) 0%, transparent 60%);
  pointer-events: none;
}

.weather-widget:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 0 16px 36px rgba(67,97,238,0.35);
  border-color: rgba(165,180,252,0.4);
}

.weather-widget .temp {
  font-family: 'Sora', sans-serif;
  font-size: 2rem;
  font-weight: 800;
  line-height: 1;
  letter-spacing: -1px;
}

.weather-widget .condition {
  font-size: 0.8rem;
  opacity: 0.8;
  margin-top: 4px;
  font-weight: 400;
}

.weather-widget i {
  font-size: 2.4rem;
  opacity: 0.9;
  filter: drop-shadow(0 2px 8px rgba(255,215,0,0.4));
  transition: transform 0.5s ease;
}

.weather-widget:hover i { transform: rotate(20deg) scale(1.15); }

/* ===== MAIN CONTENT ===== */
.main-content {
  padding: 36px 40px;
  position: relative;
  z-index: 1;
}

/* ===== WELCOME SECTION ===== */
.welcome-section {
  background: linear-gradient(130deg, #4361ee 0%, #7b2ff7 50%, #3a0ca3 100%);
  color: white;
  padding: 36px 40px;
  border-radius: 28px;
  margin-bottom: 28px;
  position: relative;
  overflow: hidden;
  box-shadow: 0 20px 60px rgba(67,97,238,0.35);
  transition: var(--transition);
}

.welcome-section::before {
  content: '';
  position: absolute;
  top: -80px; right: -60px;
  width: 280px; height: 280px;
  background: radial-gradient(circle, rgba(255,255,255,0.12) 0%, transparent 70%);
  pointer-events: none;
}

.welcome-section::after {
  content: '';
  position: absolute;
  bottom: -60px; left: 40%;
  width: 200px; height: 200px;
  background: radial-gradient(circle, rgba(6,214,160,0.15) 0%, transparent 70%);
  pointer-events: none;
}

.welcome-section:hover { transform: translateY(-4px); box-shadow: 0 28px 70px rgba(67,97,238,0.45); }

.welcome-tag {
  display: inline-flex;
  align-items: center;
  gap: 7px;
  background: rgba(255,255,255,0.15);
  border: 1px solid rgba(255,255,255,0.2);
  padding: 5px 14px;
  border-radius: 50px;
  font-size: 0.8rem;
  font-weight: 600;
  letter-spacing: 0.8px;
  text-transform: uppercase;
  margin-bottom: 16px;
}

.welcome-tag i { font-size: 0.7rem; color: #06d6a0; }

.welcome-section h1 {
  font-family: 'Sora', sans-serif;
  font-size: 2rem;
  font-weight: 800;
  margin-bottom: 8px;
  letter-spacing: -0.5px;
  position: relative;
  z-index: 1;
}

.welcome-section p {
  font-size: 0.95rem;
  opacity: 0.82;
  position: relative;
  z-index: 1;
}

.welcome-stats {
  display: flex;
  gap: 20px;
  margin-top: 24px;
  position: relative;
  z-index: 1;
  flex-wrap: wrap;
}

.welcome-stat {
  background: rgba(255,255,255,0.12);
  border: 1px solid rgba(255,255,255,0.15);
  border-radius: 14px;
  padding: 12px 20px;
  backdrop-filter: blur(4px);
  transition: var(--transition);
}

.welcome-stat:hover {
  background: rgba(255,255,255,0.2);
  transform: translateY(-2px);
}

.welcome-stat .stat-num {
  font-family: 'Sora', sans-serif;
  font-size: 1.4rem;
  font-weight: 800;
  line-height: 1;
}

.welcome-stat .stat-label {
  font-size: 0.75rem;
  opacity: 0.75;
  margin-top: 3px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

/* ===== ACTION BUTTONS ===== */
.action-buttons {
  display: flex;
  gap: 14px;
  margin-bottom: 32px;
}

.btn-chatbot {
  background: linear-gradient(135deg, #7b2ff7, #4361ee);
  border: none;
  padding: 16px 30px;
  border-radius: var(--radius-sm);
  color: white;
  font-family: 'Sora', sans-serif;
  font-weight: 600;
  font-size: 0.95rem;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  flex: 1;
  cursor: pointer;
  transition: var(--transition);
  box-shadow: 0 10px 30px rgba(123,47,247,0.3);
  position: relative;
  overflow: hidden;
  letter-spacing: 0.2px;
}

.btn-chatbot::before {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(135deg, rgba(255,255,255,0.15) 0%, transparent 60%);
  pointer-events: none;
}

.btn-chatbot::after {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(135deg, #06d6a0, #4361ee);
  opacity: 0;
  transition: opacity 0.35s ease;
}

.btn-chatbot:hover { transform: translateY(-4px) scale(1.02); box-shadow: 0 20px 40px rgba(123,47,247,0.45); }
.btn-chatbot:hover::after { opacity: 1; }
.btn-chatbot span, .btn-chatbot i { position: relative; z-index: 1; }

.btn-refresh {
  background: white;
  border: 2px solid #e5e9f8;
  padding: 16px 22px;
  border-radius: var(--radius-sm);
  color: var(--text-secondary);
  font-weight: 600;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  cursor: pointer;
  transition: var(--transition);
  box-shadow: 0 4px 16px rgba(0,0,0,0.06);
  min-width: 58px;
}

.btn-refresh:hover {
  border-color: var(--brand);
  color: var(--brand);
  transform: translateY(-3px);
  box-shadow: 0 10px 24px rgba(67,97,238,0.18);
}

@keyframes spin { to { transform: rotate(360deg); } }

/* ===== SECTION TITLE ===== */
.section-title {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 22px;
  position: relative;
}

.section-title-icon {
  width: 44px;
  height: 44px;
  background: linear-gradient(135deg, #eef1ff, #dde3ff);
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 4px 12px rgba(67,97,238,0.12);
  flex-shrink: 0;
}

.section-title-icon i { color: var(--brand); font-size: 1.15rem; }

.section-title h3 {
  font-family: 'Sora', sans-serif;
  font-size: 1.3rem;
  font-weight: 700;
  color: var(--text-primary);
  margin: 0;
  letter-spacing: -0.3px;
}

.section-title-line {
  flex: 1;
  height: 2px;
  background: linear-gradient(90deg, rgba(67,97,238,0.15), transparent);
  border-radius: 2px;
}

/* ===== SEARCH BAR ===== */
.search-wrapper {
  background: white;
  border: 2px solid #eaeef8;
  border-radius: var(--radius-sm);
  display: flex;
  align-items: center;
  padding: 0 18px;
  gap: 12px;
  margin-bottom: 28px;
  transition: var(--transition);
  box-shadow: 0 4px 16px rgba(0,0,0,0.04);
}

.search-wrapper:focus-within {
  border-color: var(--brand);
  box-shadow: 0 4px 24px rgba(67,97,238,0.15);
}

.search-wrapper i { color: var(--text-muted); transition: color 0.25s; font-size: 1rem; }
.search-wrapper:focus-within i { color: var(--brand); }

#searchCounselors {
  border: none;
  outline: none;
  flex: 1;
  padding: 16px 0;
  font-family: 'DM Sans', sans-serif;
  font-size: 0.95rem;
  color: var(--text-primary);
  background: transparent;
}

#searchCounselors::placeholder { color: var(--text-muted); }

/* ===== COUNSELORS GRID ===== */
#counselorsGrid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 22px;
}

/* Reset bootstrap row */
#counselorsGrid.row { margin: 0; }
#counselorsGrid .col-md-6, #counselorsGrid .col-lg-4 { padding: 0; width: auto; flex: none; }

/* ===== COUNSELOR CARD ===== */
#counselorsGrid .card {
  background: white;
  border: 1.5px solid #eaeef8;
  border-radius: 24px;
  box-shadow: 0 4px 20px rgba(0,0,0,0.05);
  transition: all 0.4s cubic-bezier(0.34, 1.4, 0.64, 1);
  overflow: hidden;
  cursor: pointer;
  position: relative;
  height: 100%;
}

#counselorsGrid .card::before {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0;
  height: 4px;
  background: linear-gradient(90deg, #4361ee, #7b2ff7, #06d6a0);
  opacity: 0;
  transition: opacity 0.35s ease;
}

#counselorsGrid .card:hover::before { opacity: 1; }

#counselorsGrid .card:hover {
  border-color: rgba(67,97,238,0.25);
  box-shadow: 0 20px 50px rgba(67,97,238,0.18);
  transform: translateY(-10px) scale(1.02);
}

#counselorsGrid .card-body {
  padding: 28px 22px 24px;
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
}

/* ===== AVATAR WRAPPER ===== */
.avatar-wrap {
  position: relative;
  display: inline-block;
  margin-bottom: 16px;
}

#counselorsGrid .rounded-circle {
  width: 88px;
  height: 88px;
  border: 4px solid white;
  box-shadow: 0 8px 24px rgba(0,0,0,0.12);
  object-fit: cover;
  border-radius: 50%;
  transition: all 0.4s ease;
}

#counselorsGrid .card:hover .rounded-circle {
  box-shadow: 0 12px 32px rgba(67,97,238,0.25);
  transform: scale(1.05);
}

/* ===== COUNSELOR NAME ===== */
#counselorsGrid h5 {
  font-family: 'Sora', sans-serif;
  font-size: 1.08rem;
  font-weight: 700;
  color: var(--text-primary);
  letter-spacing: -0.2px;
  margin-bottom: 10px;
}

/* ===== SPECIALIZATION BADGE ===== */
#counselorsGrid .specialization-badge {
  color: var(--brand);
  font-size: 0.8rem;
  font-weight: 600;
  background: linear-gradient(135deg, #eef1ff, #e3e8ff);
  padding: 6px 16px;
  border-radius: 50px;
  display: inline-block;
  margin-bottom: 8px;
  border: 1px solid rgba(67,97,238,0.15);
  transition: all 0.3s ease;
}

#counselorsGrid .card:hover .specialization-badge {
  background: linear-gradient(135deg, #4361ee, #7b2ff7);
  color: white;
  border-color: transparent;
}

/* ===== EDUCATION ===== */
#counselorsGrid .education-text {
  color: var(--text-muted);
  font-size: 0.82rem;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  margin-bottom: 12px;
}

#counselorsGrid .education-text i { color: var(--brand); opacity: 0.7; }

/* ===== RATING ===== */
.counselor-rating {
  margin: 10px 0 18px;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 3px;
}

.counselor-rating .fa-star.text-warning,
.counselor-rating .fa-star-half-alt.text-warning { color: var(--gold) !important; font-size: 15px; }
.counselor-rating .fa-star.text-muted,
.counselor-rating .far.fa-star { color: #e5e7eb !important; font-size: 15px; }

/* ===== STATUS DOT ===== */
.counselor-status-dot {
  position: absolute;
  bottom: 5px; right: 5px;
  width: 16px; height: 16px;
  border-radius: 50%;
  border: 3px solid white;
  box-shadow: 0 2px 6px rgba(0,0,0,0.15);
}

.dot-online { background: #06d6a0; animation: pulse 2s infinite; }
.dot-offline { background: #cbd5e1; }

/* ===== ONLINE LABEL ===== */
.online-status-label {
  display: inline-flex;
  align-items: center;
  gap: 5px;
  font-size: 0.75rem;
  font-weight: 600;
  padding: 4px 12px;
  border-radius: 50px;
  margin-bottom: 4px;
  letter-spacing: 0.3px;
}

.label-online {
  background: rgba(6,214,160,0.1);
  color: #059669;
  border: 1px solid rgba(6,214,160,0.3);
}

.label-offline {
  background: rgba(203,213,225,0.2);
  color: #94a3b8;
  border: 1px solid rgba(203,213,225,0.4);
}

/* ===== CHAT BUTTON ===== */
#counselorsGrid .btn-chat {
  background: linear-gradient(135deg, #4361ee, #7b2ff7);
  border: none;
  padding: 13px 20px;
  border-radius: 50px;
  font-size: 0.88rem;
  font-weight: 600;
  font-family: 'Sora', sans-serif;
  color: white;
  box-shadow: 0 8px 20px rgba(67,97,238,0.25);
  cursor: pointer;
  transition: all 0.35s ease;
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  letter-spacing: 0.2px;
  position: relative;
  overflow: hidden;
  margin-top: auto;
}

#counselorsGrid .btn-chat::before {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(135deg, rgba(255,255,255,0.15) 0%, transparent 60%);
}

#counselorsGrid .btn-chat:hover {
  transform: scale(1.04) translateY(-2px);
  box-shadow: 0 16px 36px rgba(67,97,238,0.4);
}

.btn-chat-offline {
  background: linear-gradient(135deg, #94a3b8, #64748b) !important;
  box-shadow: 0 8px 20px rgba(100,116,139,0.2) !important;
}

.counselor-offline-card { opacity: 0.88; }
.counselor-offline-card:hover { opacity: 1; }

/* ===== EMPTY STATE ===== */
.empty-state {
  text-align: center;
  padding: 70px 30px;
  background: white;
  border-radius: 24px;
  width: 100%;
  border: 2px dashed #e2e8f0;
  grid-column: 1 / -1;
}

.empty-state-icon {
  width: 80px;
  height: 80px;
  background: linear-gradient(135deg, #eef1ff, #dde3ff);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto 20px;
}

.empty-state-icon i { font-size: 2rem; color: var(--brand); opacity: 0.6; }

/* ===== TOAST ===== */
.toast-notification {
  position: fixed;
  bottom: 28px;
  right: 28px;
  background: white;
  border-radius: 16px;
  padding: 16px 24px;
  box-shadow: 0 16px 48px rgba(0,0,0,0.15);
  display: flex;
  align-items: center;
  gap: 14px;
  transform: translateX(120%) translateY(10px);
  transition: transform 0.4s cubic-bezier(0.34,1.4,0.64,1), opacity 0.4s ease;
  z-index: 9999;
  border-left: 4px solid var(--brand);
  max-width: 340px;
}

.toast-notification.show { transform: translateX(0) translateY(0); }
.toast-notification i { font-size: 1.4rem; color: var(--brand); flex-shrink: 0; }
.toast-notification .toast-title { font-family: 'Sora', sans-serif; font-weight: 700; color: var(--text-primary); margin-bottom: 3px; font-size: 0.9rem; }
.toast-notification .toast-message { color: var(--text-secondary); font-size: 0.82rem; }

/* ===== LOADING ===== */
.loading-overlay {
  position: fixed;
  inset: 0;
  background: rgba(240,242,249,0.75);
  backdrop-filter: blur(8px);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 10000;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
}

.loading-overlay.show { opacity: 1; visibility: visible; }

.loading-spinner {
  width: 52px;
  height: 52px;
  border: 4px solid #e0e7ff;
  border-top-color: var(--brand);
  border-right-color: #7b2ff7;
  border-radius: 50%;
  animation: spin 0.9s linear infinite;
  box-shadow: 0 8px 24px rgba(67,97,238,0.2);
}

/* ===== RATING MODAL ===== */
#ratingModal {
  display: none;
  position: fixed;
  inset: 0;
  z-index: 99999;
  align-items: center;
  justify-content: center;
}

#ratingModal.show { display: flex; }

.rating-overlay {
  position: absolute;
  inset: 0;
  background: rgba(15,22,40,0.55);
  backdrop-filter: blur(6px);
  animation: fadeIn 0.3s ease;
}

@keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }

.rating-box {
  position: relative;
  background: white;
  border-radius: 28px;
  padding: 40px 44px 36px;
  width: 400px;
  max-width: 95vw;
  text-align: center;
  box-shadow: 0 40px 80px rgba(0,0,0,0.25);
  animation: slideUp 0.4s cubic-bezier(0.34,1.56,0.64,1);
  z-index: 1;
  border: 1.5px solid #eaeef8;
}

@keyframes slideUp {
  from { transform: translateY(50px) scale(0.95); opacity: 0; }
  to { transform: translateY(0) scale(1); opacity: 1; }
}

.rating-box h4 {
  font-family: 'Sora', sans-serif;
  font-size: 1.35rem;
  font-weight: 800;
  color: var(--text-primary);
  margin-bottom: 8px;
  letter-spacing: -0.3px;
}

.rating-box p { color: var(--text-secondary); font-size: 0.93rem; margin-bottom: 24px; }

.star-row {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 28px;
}

.star-row i {
  font-size: 2.4rem;
  color: #e5e7eb;
  cursor: pointer;
  transition: color 0.15s ease, transform 0.2s cubic-bezier(0.34,1.56,0.64,1);
}

.star-row i.active, .star-row i:hover { color: var(--gold); transform: scale(1.2) rotate(-5deg); }

.rating-submit-btn {
  background: linear-gradient(135deg, #4361ee, #7b2ff7);
  color: white;
  border: none;
  border-radius: 50px;
  padding: 14px 40px;
  font-family: 'Sora', sans-serif;
  font-size: 0.95rem;
  font-weight: 700;
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 10px 28px rgba(67,97,238,0.3);
  width: 100%;
}

.rating-submit-btn:hover {
  transform: translateY(-3px);
  box-shadow: 0 18px 40px rgba(67,97,238,0.45);
}

.rating-skip { margin-top: 16px; font-size: 0.83rem; color: var(--text-muted); cursor: pointer; display: inline-block; transition: color 0.2s; }
.rating-skip:hover { color: var(--text-secondary); }

.rating-close-btn {
  position: absolute;
  top: 16px; right: 20px;
  background: #f0f4ff;
  border: none;
  width: 32px; height: 32px;
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  color: var(--text-muted);
  cursor: pointer;
  transition: all 0.2s;
  font-size: 0.9rem;
}

.rating-close-btn:hover { background: #e0e7ff; color: var(--text-primary); }

.rating-success { display: none; flex-direction: column; align-items: center; gap: 12px; }
.rating-success i { font-size: 3.5rem; color: #06d6a0; animation: popIn 0.4s cubic-bezier(0.34,1.56,0.64,1); }

@keyframes popIn { from { transform: scale(0) rotate(-30deg); } to { transform: scale(1) rotate(0deg); } }

/* ===== SCROLLBAR ===== */
::-webkit-scrollbar { width: 6px; height: 6px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: #d1d5e8; border-radius: 8px; }
::-webkit-scrollbar-thumb:hover { background: var(--brand); }

/* ===== RESPONSIVE ===== */
@media (max-width: 991px) {
  .main-content { padding: 24px 20px; }
  .welcome-section { padding: 28px 24px; }
  .welcome-section h1 { font-size: 1.5rem; }
  #counselorsGrid { grid-template-columns: repeat(auto-fill, minmax(240px, 1fr)); }
}

@media (max-width: 767px) {
  #counselorsGrid { grid-template-columns: 1fr; }
  .welcome-stats { flex-direction: column; gap: 12px; }
  .action-buttons { flex-direction: column; }
  .btn-refresh { width: 100%; }
}

/* ===== ENTRANCE ANIMATIONS ===== */
@keyframes fadeUp {
  from { opacity: 0; transform: translateY(24px); }
  to { opacity: 1; transform: translateY(0); }
}

.welcome-section { animation: fadeUp 0.6s ease forwards; }
.action-buttons { animation: fadeUp 0.6s 0.1s ease both; }
.section-title { animation: fadeUp 0.6s 0.2s ease both; }
.search-wrapper { animation: fadeUp 0.6s 0.25s ease both; }
</style>

<!-- Loading Overlay -->
<div class="loading-overlay" id="loadingOverlay">
    <div class="loading-spinner"></div>
</div>

<!-- Toast Notification -->
<div class="toast-notification" id="toastNotification">
    <i class="fas fa-info-circle"></i>
    <div class="toast-content">
        <div class="toast-title" id="toastTitle">Notification</div>
        <div class="toast-message" id="toastMessage">This is a notification message</div>
    </div>
</div>

<!-- ==================== RATING MODAL ==================== -->
<div id="ratingModal" data-counselor-id="" data-counselor-name="">
    <div class="rating-overlay" onclick="closeRatingModal()"></div>
    <div class="rating-box">
        <button class="rating-close-btn" onclick="closeRatingModal()"><i class="fas fa-times"></i></button>

        <div id="ratingForm">
            <div style="font-size:2.8rem; margin-bottom:12px; filter:drop-shadow(0 4px 8px rgba(251,191,36,0.3))">🌟</div>
            <h4>Rate Your Counselor</h4>
            <p id="ratingCounselorName">How was your session?</p>

            <div class="star-row" id="starRow">
                <i class="fas fa-star" data-val="1"></i>
                <i class="fas fa-star" data-val="2"></i>
                <i class="fas fa-star" data-val="3"></i>
                <i class="fas fa-star" data-val="4"></i>
                <i class="fas fa-star" data-val="5"></i>
            </div>

            <button class="rating-submit-btn" onclick="submitCounselorRating()">Submit Rating</button>
            <div class="rating-skip" onclick="closeRatingModal()">Maybe later</div>
        </div>

        <div id="ratingSuccess" class="rating-success">
            <i class="fas fa-check-circle"></i>
            <h4>Thank you!</h4>
            <p style="color:#64748b;">Your rating has been saved.</p>
        </div>
    </div>
</div>

<div class="container-fluid" style="position:relative; z-index:1;">
    <div class="row g-0">

        <!-- ===== SIDEBAR ===== -->
        <div class="col-md-3 col-lg-2 sidebar">
            <div class="user-profile-container">

                <!-- Avatar -->
                <div class="avatar-circle" onclick="showProfile()" title="View profile">
                    <i class="fas fa-user-graduate"></i>
                </div>

                <!-- Name -->
                <h5 onclick="showProfile()" style="cursor:pointer;"><%= user.username %></h5>

                <!-- Student Badge -->
                <div class="badge-student" onclick="showProfile()" style="cursor:pointer;">
                    <i class="fas fa-circle" style="font-size:7px; color:#06d6a0;"></i>
                    Student
                </div>

                <div class="sidebar-divider"></div>

                <!-- Counselor Status -->
                <div id="counselorStatus" class="counselor-status-badge counselor-offline" onclick="checkCounselorStatus()" title="Click to refresh status">
                    <span class="realtime-indicator" style="background:#6c757d;"></span>
                    <span>Checking status…</span>
                </div>

                <!-- Chatbot hint -->
                <div class="auto-chatbot-text" onclick="startChatbot()" title="Start chatbot">
                    <i class="fas fa-robot"></i>
                    Auto-chatbot when offline
                </div>

                <div class="sidebar-divider"></div>

                <!-- Weather Widget -->
                <div class="weather-widget" onclick="refreshWeather()" title="Refresh weather">
                    <div>
                        <div class="temp">27°C</div>
                        <div class="condition">Mostly clear</div>
                    </div>
                    <i class="fas fa-sun"></i>
                </div>

            </div>
        </div>

        <!-- ===== MAIN CONTENT ===== -->
        <div class="col-md-9 col-lg-10 main-content">

            <!-- Welcome Section -->
            <div class="welcome-section">
                <div class="welcome-tag">
                    <i class="fas fa-circle"></i>
                    Safe & Confidential
                </div>
                <h1>Welcome back, <%= user.username %>! 👋</h1>
                <p>A safe, confidential space for students to seek support and connect with counselors</p>
                <div class="welcome-stats">
                    <div class="welcome-stat">
                        <div class="stat-num" id="onlineCounselorCountStat">—</div>
                        <div class="stat-label">Online Now</div>
                    </div>
                    <div class="welcome-stat">
                        <div class="stat-num">24/7</div>
                        <div class="stat-label">Chatbot Available</div>
                    </div>
                    <div class="welcome-stat">
                        <div class="stat-num">100%</div>
                        <div class="stat-label">Anonymous</div>
                    </div>
                </div>
            </div>

            <!-- Action Buttons -->
            <div class="action-buttons">
                <button class="btn-chatbot" onclick="startChatbotWithAnimation(this)">
                    <i class="fas fa-robot fa-lg"></i>
                    <span>Start Chatbot Session</span>
                </button>
                <button class="btn-refresh" onclick="refreshCounselorsWithAnimation(this)" title="Refresh">
                    <i class="fas fa-sync-alt"></i>
                </button>
            </div>

            <!-- Counselors Section -->
            <div class="row">
                <div class="col-12">
                    <div class="section-title">
                        <div class="section-title-icon">
                            <i class="fas fa-comments"></i>
                        </div>
                        <h3>Online Counselors (<span id="onlineCounselorCount">0</span>)</h3>
                        <div class="section-title-line"></div>
                    </div>

                    <!-- Search -->
                    <div class="search-wrapper">
                        <i class="fas fa-search" id="searchIcon"></i>
                        <input type="text" id="searchCounselors"
                               placeholder="Search by name or specialization… (Ctrl+K)"
                               onkeyup="searchCounselors()"
                               onfocus="animateSearchFocus()"
                               onblur="animateSearchBlur()">
                    </div>

                    <!-- Grid -->
                    <div id="counselorsGrid">
                        <div style="grid-column:1/-1; text-align:center; padding:60px 0;">
                            <div class="spinner-border text-primary" role="status">
                                <span class="visually-hidden">Loading…</span>
                            </div>
                        </div>
                    </div>

                </div>
            </div>
        </div><!-- /main-content -->

    </div>
</div>

<script src="/socket.io/socket.io.js"></script>
<script>
let socket;
let allCounselors = [];
let searchTimeout;

let selectedRating = 0;
let ratingShownThisSession = false;

function showLoading() { document.getElementById('loadingOverlay').classList.add('show'); }
function hideLoading() { document.getElementById('loadingOverlay').classList.remove('show'); }

function showToast(title, message, type = 'info') {
    const toast = document.getElementById('toastNotification');
    document.getElementById('toastTitle').textContent = title;
    document.getElementById('toastMessage').textContent = message;
    const icon = toast.querySelector('i');
    const colors = { success:'#06d6a0', error:'#ff6b6b', info:'#4361ee' };
    const icons = { success:'check-circle', error:'exclamation-circle', info:'info-circle' };
    icon.className = `fas fa-${icons[type]||'info-circle'}`;
    icon.style.color = colors[type] || colors.info;
    toast.style.borderLeftColor = colors[type] || colors.info;
    toast.classList.add('show');
    setTimeout(() => toast.classList.remove('show'), 3000);
}

/* ===== RATING ===== */
document.querySelectorAll('#starRow i').forEach(star => {
    star.addEventListener('click', () => {
        selectedRating = parseInt(star.dataset.val);
        document.querySelectorAll('#starRow i').forEach((s, idx) => s.classList.toggle('active', idx < selectedRating));
    });
    star.addEventListener('mouseenter', () => {
        const val = parseInt(star.dataset.val);
        document.querySelectorAll('#starRow i').forEach((s, idx) => s.style.color = idx < val ? '#fbbf24' : '#e5e7eb');
    });
    star.addEventListener('mouseleave', () => {
        document.querySelectorAll('#starRow i').forEach((s, idx) => s.style.color = idx < selectedRating ? '#fbbf24' : '#e5e7eb');
    });
});

async function maybeShowRatingModal(counselorId, counselorName, messageCount) {
    if (ratingShownThisSession) return;
    if (messageCount < 10) return;
    try {
        const res = await fetch(`/api/ratings/check/${counselorId}`, { credentials: 'same-origin' });
        const data = await res.json();
        if (data.hasRated) return;
    } catch(e) { return; }
    ratingShownThisSession = true;
    openRatingModal(counselorId, counselorName);
}

function openRatingModal(counselorId, counselorName) {
    selectedRating = 0;
    document.querySelectorAll('#starRow i').forEach(s => { s.classList.remove('active'); s.style.color = '#e5e7eb'; });
    document.getElementById('ratingForm').style.display = 'block';
    document.getElementById('ratingSuccess').style.display = 'none';
    document.getElementById('ratingCounselorName').textContent = `How was your session with ${counselorName}?`;
    const modal = document.getElementById('ratingModal');
    modal.dataset.counselorId = counselorId;
    modal.dataset.counselorName = counselorName;
    modal.classList.add('show');
}

function closeRatingModal() { document.getElementById('ratingModal').classList.remove('show'); }

async function submitCounselorRating() {
    if (!selectedRating) { showToast('Select Stars', 'Please select a star rating first.', 'error'); return; }
    const modal = document.getElementById('ratingModal');
    const counselorId = modal.dataset.counselorId;
    try {
        const res = await fetch('/api/ratings/counselor', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            credentials: 'same-origin',
            body: JSON.stringify({ counselorId, rating: selectedRating })
        });
        const data = await res.json();
        if (data.success) {
            document.getElementById('ratingForm').style.display = 'none';
            document.getElementById('ratingSuccess').style.display = 'flex';
            updateCardRating(counselorId, data.avgRating);
            setTimeout(closeRatingModal, 2200);
        } else { showToast('Error', data.message || 'Could not submit rating.', 'error'); }
    } catch(e) { showToast('Error', 'Server error. Please try again.', 'error'); }
}

function updateCardRating(counselorId, newAvg) {
    const card = document.querySelector(`[data-counselor-id="${counselorId}"] .counselor-rating`);
    if (card) card.innerHTML = generateStars(newAvg);
    const c = allCounselors.find(x => String(x._id) === String(counselorId));
    if (c) c.rating = newAvg;
}

/* ===== SOCKET ===== */

document.addEventListener('DOMContentLoaded', function() {
    // ✅ Always hide loading overlay on page load (catches stuck state from startChat)
    hideLoading();

    initializeSocket();
    checkCounselorStatus();
    setInterval(checkCounselorStatus, 10000);
    observeCards();

    // Rating trigger on return
    const chatId = sessionStorage.getItem('currentChatId');
    const counselorId = sessionStorage.getItem('currentCounselorId');
    const counselorName = sessionStorage.getItem('currentCounselorName');
    const count = parseInt(sessionStorage.getItem('studentMsgCount') || '0');
    if (chatId && counselorId && count >= 10) {
        setTimeout(() => maybeShowRatingModal(counselorId, counselorName || 'your counselor', count), 1500);
    }
});
window.addEventListener('pageshow', function(event) {
    // event.persisted = true means page was restored from bfcache (Back button)
    if (event.persisted) {
        hideLoading(); // ✅ Clear any stuck loading overlay
        checkCounselorStatus(); // ✅ Reload counselors fresh
    }
});

function initializeSocket() {
    socket = io("http://localhost:5000", { auth: { userId: "<%= user._id %>", role: "student" } });

    socket.on('connect', () => {
        if (typeof currentIssueId !== 'undefined' && currentIssueId) socket.emit('join-chat', currentIssueId);
    });

    socket.on("newMessage", (msg) => {
        if (msg.chatId !== currentIssueId) { loadConversations(); return; }
        if (!selectedIssue) return;
        selectedIssue.messages = selectedIssue.messages || [];
        const exists = selectedIssue.messages.some(m => String(m._id) === String(msg._id));
        if (!exists) {
            selectedIssue.messages.push(msg);
            displayMessages(selectedIssue.messages, selectedIssue);
            const container = document.getElementById('chatMessages');
            container.scrollTop = container.scrollHeight;
        }
        loadConversations();
    });

    socket.on('user-status-change', (data) => {
        if (data.role === 'counselor') { checkCounselorStatus(); showToast('Status Update', 'Counselor status changed', 'info'); }
    });

    socket.on("counselorProfileUpdated", () => {
        checkCounselorStatus();
        showToast('Profile Updated', 'Counselor information has been updated', 'info');
    });

    socket.on("online-users", async (users) => {
        const el = document.getElementById("onlineCounselorCount");
        if (el) el.innerText = users.length;
        document.querySelectorAll("#counselorsGrid .card").forEach(card => {
            const dot = card.querySelector(".realtime-indicator");
            const counselorId = card.getAttribute("data-id");
            if (dot) dot.style.display = users.includes(counselorId) ? "inline-block" : "none";
        });
        try {
            const response = await fetch("/api/counselors/online");
            const onlineCounselors = await response.json();
            updateCounselorCards(onlineCounselors);
        } catch(err) { console.error("Error fetching online counselors:", err); }
    });
}
// REPLACE the existing checkCounselorStatus function with this:
async function checkCounselorStatus() {
    try {
        const [allRes, onlineRes] = await Promise.all([
            fetch('/api/counselors/all', { credentials: 'same-origin' }),
            fetch('/api/counselors/online', { credentials: 'same-origin' })
        ]);
        const allData = await allRes.json();
        const onlineData = await onlineRes.json();
        const onlineIds = new Set((onlineData.success ? onlineData.counselors : []).map(c => String(c._id)));
        if (allData.success) {
            allCounselors = allData.counselors.map(c => ({ ...c, isOnline: onlineIds.has(String(c._id)) }));
            const onlineCount = allCounselors.filter(c => c.isOnline).length;
            updateCounselorStatus(onlineCount > 0, onlineCount);
            displayCounselors(allCounselors);
        } else {
            updateCounselorStatus(false, 0);
            displayCounselors([]);
        }
    } catch(error) {
        console.error('Status check error:', error);
        updateCounselorStatus(false, 0);
        displayCounselors([]);
        showToast('Error', 'Failed to load counselors', 'error');
    } finally {
        // ✅ ALWAYS hide loading overlay, no matter what
        hideLoading();
    }
}
function updateCounselorStatus(isOnline, count) {
    const statusEl = document.getElementById('counselorStatus');
    const statEl = document.getElementById('onlineCounselorCountStat');
    if (statEl) statEl.textContent = count;
    if (isOnline) {
        statusEl.className = 'counselor-status-badge counselor-online';
        statusEl.innerHTML = `<span class="realtime-indicator"></span><span class="fw-bold">${count} Counselor${count>1?'s':''} Online</span>`;
    } else {
        statusEl.className = 'counselor-status-badge counselor-offline';
        statusEl.innerHTML = `<span class="realtime-indicator" style="background:#6c757d;animation:none;"></span><span>Offline — Chatbot Active</span>`;
    }
}

function generateStars(rating) {
    let stars = '';
    const full = Math.floor(rating);
    const half = rating % 1 >= 0.5;
    for (let i = 1; i <= 5; i++) {
        if (i <= full) stars += '<i class="fas fa-star text-warning"></i>';
        else if (i === full + 1 && half) stars += '<i class="fas fa-star-half-alt text-warning"></i>';
        else stars += '<i class="far fa-star text-muted"></i>';
    }
    return stars;
}

function displayCounselors(counselors) {
    const grid = document.getElementById('counselorsGrid');
    const countEl = document.getElementById('onlineCounselorCount');
    if (countEl) countEl.textContent = counselors.length;
    const statEl = document.getElementById('onlineCounselorCountStat');
    if (statEl) statEl.textContent = counselors.filter(c => c.isOnline).length;

    if (counselors.length === 0) {
        grid.innerHTML = `
            <div class="empty-state" style="grid-column:1/-1;">
                <div class="empty-state-icon"><i class="fas fa-user-md"></i></div>
                <h5 style="font-family:'Sora',sans-serif;font-weight:700;color:#1a2639;margin-bottom:8px;">No counselors available</h5>
                <p style="color:#94a3b8;margin-bottom:20px;">Our chatbot is here for you 24/7</p>
                <button class="btn-chatbot" onclick="startChatbot()" style="display:inline-flex;padding:13px 28px;width:auto;border-radius:50px;">
                    <i class="fas fa-robot me-2"></i>Start Chatbot
                </button>
            </div>`;
        return;
    }

    let html = '';
    counselors.forEach(c => {
        const fullName = `${c.firstName||''} ${c.lastName||''}`.trim() || 'Counselor';
        const firstNameOnly = c.firstName || 'Counselor';
        const photo = c.profilePhoto ? `/uploads/images/${c.profilePhoto}` : '/images/default-avatar.png';
        const rating = c.rating || 4.5;
        const spec = c.specialization || 'Stress & Anxiety Management';
        const edu = c.education || 'Diploma in Counseling Psychology';

        html += `
        <div data-counselor-id="${c._id}">
            <div class="card ${!c.isOnline ? 'counselor-offline-card' : ''}" onclick="viewCounselorProfile('${c._id}')">
                <div class="card-body">
                    <div class="avatar-wrap">
                        <img src="${photo}" class="rounded-circle" width="88" height="88" alt="${fullName}" onerror="this.src='/images/default-avatar.png'">
                        <span class="counselor-status-dot ${c.isOnline ? 'dot-online' : 'dot-offline'}"></span>
                    </div>
                    <h5>${fullName}</h5>
                    <div class="mb-1">
                        <span class="online-status-label ${c.isOnline ? 'label-online' : 'label-offline'}">
                            <i class="fas fa-circle" style="font-size:6px;"></i>
                            ${c.isOnline ? 'Online' : 'Offline'}
                        </span>
                    </div>
                    <div class="specialization-badge">${spec}</div>
                    <div class="education-text"><i class="fas fa-graduation-cap"></i>${edu}</div>
                    <div class="counselor-rating">${generateStars(rating)}</div>
                    <button class="btn-chat ${!c.isOnline ? 'btn-chat-offline' : ''}"
                        onclick="event.stopPropagation(); startChat('${c._id}', '${fullName.replace(/'/g,"\\'")}', ${c.isOnline})">
                        <i class="fas fa-comment-dots"></i>Chat with ${firstNameOnly}
                    </button>
                </div>
            </div>
        </div>`;
    });

    grid.innerHTML = html;
    observeCards();
}

function updateCounselorCards(onlineCounselors) { /* handled by full re-render */ }

function searchCounselors() {
    clearTimeout(searchTimeout);
    searchTimeout = setTimeout(() => {
        const term = document.getElementById('searchCounselors').value.toLowerCase();
        const filtered = allCounselors.filter(c => {
            const name = `${c.firstName||''} ${c.lastName||''}`.toLowerCase();
            return name.includes(term) || (c.specialization||'').toLowerCase().includes(term);
        });
        displayCounselors(filtered);
        if (term.length > 0) showToast('Search', `Found ${filtered.length} counselor${filtered.length !== 1 ? 's' : ''}`, 'info');
    }, 300);
}

function animateSearchFocus() {
    document.getElementById('searchIcon').style.color = '#4361ee';
    document.getElementById('searchIcon').style.transform = 'scale(1.1)';
}

function animateSearchBlur() {
    document.getElementById('searchIcon').style.color = '#9aa3c0';
    document.getElementById('searchIcon').style.transform = 'scale(1)';
}

function refreshCounselorsWithAnimation(btn) {
    btn.querySelector('i').classList.add('fa-spin');
    showLoading();
    checkCounselorStatus().finally(() => {
        setTimeout(() => {
            btn.querySelector('i').classList.remove('fa-spin');
            hideLoading();
            showToast('Refreshed', 'Counselors list updated', 'success');
        }, 500);
    });
}

function startChatbotWithAnimation(btn) {
    const icon = btn.querySelector('i');
    icon.style.transform = 'rotate(15deg) scale(1.2)';
    setTimeout(() => { icon.style.transform = ''; startChatbot(); }, 200);
}

function observeCards() {
    const cards = document.querySelectorAll('#counselorsGrid .card');
    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.style.opacity = '1';
                entry.target.style.transform = 'translateY(0) scale(1)';
            }
        });
    }, { threshold: 0.08 });
    cards.forEach((card, i) => {
        card.style.opacity = '0';
        card.style.transform = 'translateY(28px) scale(0.97)';
        card.style.transition = `opacity 0.5s ${i * 0.07}s ease, transform 0.5s ${i * 0.07}s cubic-bezier(0.34,1.3,0.64,1)`;
        observer.observe(card);
    });
}

function viewCounselorProfile(counselorId) { showToast('Profile', 'Viewing counselor profile', 'info'); }
function showProfile() { showToast('Profile', 'Viewing your profile', 'info'); }
function refreshWeather() { showToast('Weather', 'Weather information updated', 'success'); }

// REPLACE the existing startChat function with this:
async function startChat(counselorId, counselorName, isOnline = false) {
    if (!isOnline) showToast('Counselor Offline', `${counselorName} is offline. Message saved — they will reply when available.`, 'info');
    try {
        showLoading();
        const res = await fetch('/api/chat/start', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            credentials: 'same-origin',
            body: JSON.stringify({ counselorId })
        });
        const data = await res.json();
        if (data.success) {
            sessionStorage.setItem('currentChatId', data.chatId);
            sessionStorage.setItem('currentCounselorId', counselorId);
            sessionStorage.setItem('currentCounselorName', counselorName);
            sessionStorage.setItem('anonymousId', data.anonymousId);
            // ✅ Don't hide loading here — let pageshow handle it on return
            window.location.href = `/chat/${data.chatId}`;
        } else {
            hideLoading(); // ✅ Only hide on failure
            showToast('Error', data.message || 'Unable to start chat', 'error');
        }
    } catch(err) {
        hideLoading(); // ✅ Always hide on error
        showToast('Error', 'Server error. Please try again.', 'error');
    }
}

function startChatbot() {
    showLoading();
    showToast('Chatbot', 'Starting anonymous chatbot session...', 'info');
    setTimeout(() => { hideLoading(); window.location.href = '/chatbot'; }, 500);
}

document.getElementById('searchCounselors')?.addEventListener('keypress', function(e) {
    if (e.key === 'Enter') { searchCounselors(); showToast('Search', 'Searching counselors...', 'info'); }
});

document.addEventListener('keydown', function(e) {
    if (e.ctrlKey && e.key === 'k') { e.preventDefault(); document.getElementById('searchCounselors').focus(); }
});
</script>

<%- include('partials/footer') %>
